---
import { Icon } from 'astro-icon/components';
import Button from '~/components/ui/Button.astro';
import Headline from '~/components/ui/Headline.astro';
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';
import Image from '~/components/common/Image.astro';
import type { Content as Props } from '~/types';

const {
  title = await Astro.slots.render('title'),
  subtitle = await Astro.slots.render('subtitle'),
  tagline,
  content = await Astro.slots.render('content'),
  callToAction,
  items = [],
  columns,
  image = await Astro.slots.render('image'),
  isReversed = false,
  isBeforeContent = false,
  isAfterContent = false,

  id,
  isDark = false,
  classes = {},
  bg = await Astro.slots.render('bg'),
} = Astro.props;

const imageObject = typeof image === 'object' && image !== null ? (image as Record<string, unknown>) : null;
let cleanImage = image;
let caption = '';
let captionJustification = 'right';

if (imageObject) {
  caption = typeof imageObject.caption === 'string' ? imageObject.caption : '';
  captionJustification =
    typeof imageObject.captionJustification === 'string' ? imageObject.captionJustification : 'right';
  const rest = { ...imageObject };
  delete rest.caption;
  delete rest.captionJustification;
  cleanImage = rest;
}

const columnsClass =
  columns === 4
    ? 'lg:grid-cols-4 md:grid-cols-3 sm:grid-cols-2'
    : columns === 3
      ? 'lg:grid-cols-3 sm:grid-cols-2'
      : columns === 2
        ? 'sm:grid-cols-2'
        : '';
---

<WidgetWrapper
  id={id}
  isDark={isDark}
  containerClass={`max-w-7xl mx-auto ${isBeforeContent ? 'pb-0 md:pb-0 lg:pb-0' : ''} ${isAfterContent ? 'pt-0 md:pt-0 lg:pt-0' : ''} ${classes?.container ?? ''}`}
  bg={bg}
>
  <Headline
    title={title}
    subtitle={subtitle}
    tagline={tagline}
    classes={{
      container: 'max-w-xl sm:mx-auto lg:max-w-2xl',
      title: 'text-4xl md:text-5xl font-bold tracking-tighter mb-4 font-heading',
      subtitle: 'max-w-3xl mx-auto sm:text-center text-xl text-muted dark:text-slate-400',
    }}
  />
  <div class="mx-auto max-w-7xl p-4 md:px-8">
    <div class={`md:flex ${isReversed ? 'md:flex-row-reverse' : ''} md:gap-16`}>
      <div class="md:basis-1/2 self-center">
        {content && <div class="mb-12 text-lg dark:text-slate-400" set:html={content} />}

        {
          callToAction && (
            <div class="mt-[-40px] mb-8 text-primary">
              <Button variant="link" {...callToAction} />
            </div>
          )
        }

        {
          items.length > 0 && (
            <div class:list={['grid mx-auto gap-y-4 md:gap-y-8 gap-8', columnsClass]}>
              {items.map(({ title: itemTitle, description, icon, callToAction }) => (
                <div class="intersect-once motion-safe:md:opacity-0 motion-safe:md:intersect:animate-fade">
                  <div class="flex flex-row max-w-none">
                    <div class="flex justify-center">
                      <Icon
                        name={icon || 'tabler:disc'}
                        class="flex h-7 w-7 items-center justify-center rounded-full bg-stone-600 dark:bg-stone-700 text-stone-50 p-1 mr-2 rtl:mr-0 rtl:ml-2"
                      />
                    </div>
                    <div class="mt-0.5">
                      {itemTitle && (
                        <h3 class="text-lg font-medium leading-6 dark:text-white ml-2 rtl:ml-0 rtl:mr-2">
                          {itemTitle}
                        </h3>
                      )}
                      {description && (
                        <p
                          class:list={[{ 'mt-3': itemTitle }, 'text-muted dark:text-slate-400 ml-2 rtl:ml-0 rtl:mr-2']}
                          set:html={description}
                        />
                      )}
                      {callToAction && (
                        <div
                          class:list={[
                            { 'mt-3': itemTitle || description },
                            'text-lg font-medium leading-6 dark:text-white ml-2 rtl:ml-0 rtl:mr-2',
                          ]}
                        >
                          <Button variant="link" {...callToAction} />
                        </div>
                      )}
                    </div>
                  </div>
                </div>
              ))}
            </div>
          )
        }
      </div>
      <div aria-hidden="true" class="mt-10 md:mt-0 md:basis-1/2">
        {
          image && (
            <div class="relative m-auto max-w-4xl">
              {typeof image === 'string' ? (
                <Fragment set:html={image} />
              ) : (
                <>
                  <Image
                    class="mx-auto w-full rounded-lg bg-gray-500 shadow-lg"
                    width={500}
                    height={500}
                    widths={[400, 768]}
                    sizes="(max-width: 768px) 100vw, 432px"
                    layout="responsive"
                    format="webp"
                    {...(cleanImage as Record<string, unknown>)}
                  />
                  {caption && (
                    <p
                      class:list={[
                        'text-xs text-muted mt-1.5 px-1 font-medium tracking-tight',
                        {
                          'text-left': captionJustification === 'left',
                          'text-center': captionJustification === 'center',
                          'text-right': captionJustification === 'right',
                        },
                      ]}
                    >
                      {caption}
                    </p>
                  )}
                </>
              )}
            </div>
          )
        }
      </div>
    </div>
  </div>
</WidgetWrapper>
